{
  "name": "forecastx-platform",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "sync-api": "node scripts/sync-api.js",
    "build": "node scripts/sync-api.js && cd apps/web && npm install && npm run build",
    "vercel-build": "node scripts/sync-api.js && cd apps/web && npm install && npm run build"
  }
}
